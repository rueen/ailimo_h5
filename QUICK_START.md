# 艾力默用户端 - 快速启动指南

## 🚀 立即开始

### 第一步：安装依赖

```bash
cd /Users/rueen/Documents/vue/ailimo/ailimo_h5
npm install
```

### 第二步：启动开发服务器

```bash
npm run dev
```

浏览器将自动打开 http://localhost:5173

### 第三步：开始开发

项目已完全配置好,可以直接开始与后端接口联调!

## ✅ 已完成的功能

### 📱 移动端页面
- ✅ 登录/注册页面
- ✅ 首页(Banner + 服务卡片)
- ✅ 服务中心
- ✅ 设备租赁(列表/详情/预约)
- ✅ 笼位预约
- ✅ 实验代操作
- ✅ 动物订购
- ✅ 试剂耗材订购
- ✅ 我的订单(列表/详情)
- ✅ 案例展示(列表/详情)
- ✅ 公司概况
- ✅ 个人中心

### 💻 PC端适配
- ✅ 响应式布局(最大宽度1200px)
- ✅ 顶部导航栏
- ✅ 底部页脚
- ✅ 所有页面已适配PC端

## 🔧 配置说明

### API 接口地址
开发环境会自动代理到 `http://localhost:3000`

如需修改,请编辑 `vite.config.js`:

```javascript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3000',  // 修改为你的后端地址
      changeOrigin: true
    }
  }
}
```

### 主题颜色
主题颜色在 `src/styles/variables.less` 中配置:

```less
@primary-color: #00e9aa;  // 主题绿色
```

## 📝 测试账号

### 测试流程
1. 打开注册页面
2. 填写注册信息(需要真实手机号接收验证码)
3. 提交注册,等待管理员审核
4. 审核通过后,使用手机号+验证码登录
5. 开始使用各项服务

### 注意事项
- 验证码有效期: 5分钟
- 同一手机号每分钟只能发送1次验证码
- 新用户注册后需要管理员审核才能使用服务

## 📦 生产构建

```bash
# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

构建产物在 `dist` 目录

## 🌐 部署建议

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 📱 微信公众号配置

项目使用 Hash 路由,可直接配置到微信公众号菜单:

```
首页: https://yourdomain.com/#/home
服务中心: https://yourdomain.com/#/services
我的订单: https://yourdomain.com/#/orders
个人中心: https://yourdomain.com/#/my
```

## 🎯 接口联调检查清单

### 用户认证模块
- [ ] POST /api/h5/auth/send-code - 发送验证码
- [ ] POST /api/h5/auth/register - 用户注册
- [ ] POST /api/h5/auth/login - 用户登录
- [ ] GET /api/h5/auth/profile - 获取个人信息
- [ ] GET /api/h5/auth/audit-status - 查看审核状态
- [ ] POST /api/h5/auth/logout - 退出登录

### 通用接口
- [ ] GET /api/h5/regions - 获取地区列表
- [ ] GET /api/h5/organizations - 获取组织机构列表
- [ ] GET /api/h5/research-groups - 获取课题组列表
- [ ] GET /api/h5/environment-types - 获取环境类型列表
- [ ] GET /api/h5/animal-types - 获取动物类型列表

### 设备租赁
- [ ] GET /api/h5/equipment - 获取设备列表
- [ ] GET /api/h5/equipment/:id - 获取设备详情
- [ ] GET /api/h5/equipment/:id/available-slots - 查询可用时间段
- [ ] POST /api/h5/equipment-orders - 提交设备租赁订单

### 订单管理
- [ ] GET /api/h5/my-orders - 获取我的所有订单
- [ ] GET /api/h5/my-orders/:type/:id - 获取订单详情

### 其他模块
- [ ] POST /api/h5/cage-orders - 提交笼位预约订单
- [ ] POST /api/h5/experiment-orders - 提交实验代操作订单
- [ ] POST /api/h5/animal-orders - 提交动物订购订单
- [ ] POST /api/h5/reagent-orders - 提交试剂耗材订单
- [ ] GET /api/h5/cases - 获取案例列表
- [ ] GET /api/h5/cases/:id - 获取案例详情
- [ ] GET /api/h5/company-info - 获取公司信息

## 🐛 常见问题

### 1. 安装依赖失败
```bash
# 清除缓存后重试
rm -rf node_modules package-lock.json
npm install
```

### 2. 开发服务器启动失败
检查端口5173是否被占用,可以在 `vite.config.js` 中修改端口

### 3. API 请求跨域
确保 vite.config.js 中的代理配置正确

### 4. 页面空白
检查浏览器控制台错误信息,通常是路由或组件引入问题

## 📞 技术支持

如有问题,请查看:
- `PROJECT_COMPLETE.md` - 项目完整说明文档
- `docs/` 目录 - 接口文档
- `ui/` 目录 - 设计稿

---

**祝开发顺利!** 🎉
